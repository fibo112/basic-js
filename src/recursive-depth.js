module.exports = class DepthCalculator {
    calculateDepth(arr) {
        var sum = 1;
        if (!Array.isArray(arr)) {

        return 1;
        }
        }
        for (var i = 0; i < arr.length; i++)
            if (arr[i] == Array) {
                sum++;
            }
        
    }
